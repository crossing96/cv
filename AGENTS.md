# Agent instructions (CV repo)

## Resume content files

For `_data/data.yml`, `_data/**/*.yml`, and `_config.yml`:

- **Do not edit these files directly** unless the user explicitly asks you to apply or commit changes.
- **Propose changes** in chat (diff or YAML snippets) and let the user paste or approve.

## Jekyll theme

This site uses [online-cv](https://github.com/sharu725/online-cv). See `.cursor/rules/` for field naming conventions.
